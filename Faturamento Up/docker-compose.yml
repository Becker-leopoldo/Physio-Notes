services:
  faturamento:
    build:
      context: ..                        # raiz do repositório (inclui assets/)
      dockerfile: "Faturamento Up/Dockerfile"
    ports:
      - "8000:8000"
    env_file:
      - .env                                    # contém DATABASE_URL e GOOGLE_CREDENTIALS_JSON
    volumes:
      - faturamento_data:/app/data
    restart: unless-stopped

volumes:
  faturamento_data:
